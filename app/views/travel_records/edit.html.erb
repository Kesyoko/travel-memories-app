<h1 class="text-4xl font-bold mb-6 text-center font-moji">旅記録編集</h1>
<br>
<div class="mb-8 flex justify-center">
  <%= link_to '記録削除', travel_record_path(@travel_record), 
  data: { turbo_method: :delete,turbo_confirm: "削除しますか？" },
  class: "bg-red-500 hover:bg-red-700 text-siro font-bold py-2 px-4 rounded font-moji" %>
</div>
<%= render 'form',travel_record: @travel_record %>
